server {
    listen 80 default_server;
    listen [::]:80 default_server;

    root /var/www/html;
    index index.html;

    server_name _;

    location / {
        limit_except GET POST {
            deny all;
        }

        try_files $uri $uri/ =404;
    }
}
